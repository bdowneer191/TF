# This file is a template for the API keys required by the application.
# The application is designed for users to enter their own API keys in the
# settings panel of the UI. The keys are then stored in the browser's localStorage.

# These environment variables are NOT used by the application directly,
# but this file serves as a reference for which keys are needed.

# --- Google Cloud Platform Keys ---
# Used for the core AI analysis features.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# Used for the Google Custom Search API.
GOOGLE_SEARCH_API_KEY="YOUR_GOOGLE_SEARCH_API_KEY"
GOOGLE_SEARCH_ENGINE_ID="YOUR_GOOGLE_SEARCH_ENGINE_ID"

# Used for the Google Fact Check Tools API.
GOOGLE_FACT_CHECK_API_KEY="YOUR_GOOGLE_FACT_CHECK_API_KEY"

# --- Third-Party API Keys ---
# Used to fetch real-time news articles.
NEWSDATA_IO_API_KEY="YOUR_NEWSDATA_IO_API_KEY"

# (Optional) Alternative search API.
SERP_API_KEY="YOUR_SERP_API_KEY"
